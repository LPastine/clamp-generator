import React from 'react';
import { ClampFunction, Title, ClampForm } from './components';

type Props = {};

const App = (props: Props) => {
  return (
    <>
      <Title />
      <ClampForm />
    </>
  );
};

export default App;
